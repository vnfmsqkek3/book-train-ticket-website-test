# CodeBuild buildspec — 백엔드 이미지 빌드 → ECR push → ECS 배포 아티팩트 생성
# 환경변수(파이프라인에서 주입): AWS_ACCOUNT_ID, AWS_DEFAULT_REGION, ECR_REPO_URL, CONTAINER_NAME
version: 0.2

phases:
  pre_build:
    commands:
      - echo "ECR 로그인..."
      - aws ecr get-login-password --region "$AWS_DEFAULT_REGION" | docker login --username AWS --password-stdin "$AWS_ACCOUNT_ID.dkr.ecr.$AWS_DEFAULT_REGION.amazonaws.com"
      - IMAGE_TAG="${CODEBUILD_RESOLVED_SOURCE_VERSION:-latest}"
      - IMAGE_TAG="${IMAGE_TAG:0:8}"
  build:
    commands:
      - echo "백엔드 이미지 빌드 (컨텍스트: ticket-booking/)..."
      # 소스 루트에 make.md + ticket-booking/ 존재. 컨텍스트는 ticket-booking/
      - cd ticket-booking
      - docker build -f backend/Dockerfile -t "$ECR_REPO_URL:$IMAGE_TAG" -t "$ECR_REPO_URL:latest" .
  post_build:
    commands:
      - echo "이미지 push..."
      - docker push "$ECR_REPO_URL:$IMAGE_TAG"
      - docker push "$ECR_REPO_URL:latest"
      - echo "ECS 배포용 imagedefinitions.json 생성..."
      - printf '[{"name":"%s","imageUri":"%s"}]' "$CONTAINER_NAME" "$ECR_REPO_URL:$IMAGE_TAG" > imagedefinitions.json
      - cat imagedefinitions.json

artifacts:
  files:
    - ticket-booking/imagedefinitions.json
  discard-paths: yes
